import styled from 'styled-components';
import OrderTableCell from './OrderTableCell';

const OrderTableHeadCell = styled(OrderTableCell)`
  text-align: center;
  font-weight: bold;
  background: #0003;
  border-bottom: 2px solid #fff;
`;

export default OrderTableHeadCell;
